import Link from "next/link"; import { login } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="container grid min-h-[calc(100vh-64px)] place-items-center py-10"><form action={login} className="card w-full max-w-md p-7"><h1 className="text-2xl font-extrabold">เข้าสู่ระบบ KKU CARE</h1><p className="mb-2 mt-2 text-[#78716C]">สำหรับนักศึกษา บุคลากร และเจ้าหน้าที่ KKU Mail เท่านั้น</p><p className="mb-6 text-sm font-semibold text-[#A73B24]">ต้องใช้อีเมลลงท้ายด้วย @kkumail.com</p>{error&&<p role="alert" className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="field"><label htmlFor="email">อีเมล KKU Mail</label><input id="email" name="email" type="email" placeholder="ชื่อผู้ใช้@kkumail.com" required/></div><div className="field"><label htmlFor="password">รหัสผ่าน</label><input id="password" name="password" type="password" required/></div><button className="btn btn-primary w-full">เข้าสู่ระบบ</button><div className="mt-5 flex justify-between text-sm"><Link className="text-[#A73B24]" href="/forgot-password">ลืมรหัสผ่าน</Link><Link className="text-[#A73B24]" href="/register">สมัครสมาชิก</Link></div></form></main>}
