import Link from "next/link"; import { register } from "./actions";
export default async function Register({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="container grid min-h-[calc(100vh-64px)] place-items-center py-10"><form action={register} className="card w-full max-w-md p-7"><h1 className="text-2xl font-extrabold">สมัครสมาชิก</h1><p className="mb-2 mt-2 text-[#78716C]">เริ่มใช้งาน KKU CARE</p><p className="mb-6 text-sm font-semibold text-[#A73B24]">รับเฉพาะอีเมล KKU Mail (@kkumail.com)</p>{error&&<p role="alert" className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="field"><label htmlFor="full_name">ชื่อ–นามสกุล</label><input id="full_name" name="full_name" required/></div><div className="field"><label htmlFor="email">อีเมล KKU Mail</label><input id="email" name="email" type="email" placeholder="ชื่อผู้ใช้@kkumail.com" required/></div><div className="field"><label htmlFor="password">รหัสผ่าน</label><input id="password" name="password" type="password" minLength={8} required/></div><button className="btn btn-primary w-full">สมัครสมาชิก</button><p className="mt-5 text-center text-sm">มีบัญชีแล้ว? <Link className="text-[#A73B24]" href="/login">เข้าสู่ระบบ</Link></p></form></main>}
