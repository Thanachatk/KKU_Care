import Link from "next/link"; import { reset } from "./actions";
export default async function ForgotPassword({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="container grid min-h-[calc(100vh-64px)] place-items-center py-10"><form action={reset} className="card w-full max-w-md p-7"><h1 className="text-2xl font-extrabold">ลืมรหัสผ่าน</h1><p className="mb-2 mt-2 text-[#78716C]">ใช้ KKU Mail เพื่อรับลิงก์ตั้งรหัสผ่านใหม่</p><p className="mb-6 text-sm font-semibold text-[#A73B24]">เฉพาะอีเมล @kkumail.com</p>{error&&<p role="alert" className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="field"><label htmlFor="email">อีเมล KKU Mail</label><input id="email" name="email" type="email" placeholder="ชื่อผู้ใช้@kkumail.com" required/></div><button className="btn btn-primary w-full">ส่งลิงก์รีเซ็ต</button><Link className="mt-5 block text-center text-sm text-[#A73B24]" href="/login">กลับเข้าสู่ระบบ</Link></form></main>}
